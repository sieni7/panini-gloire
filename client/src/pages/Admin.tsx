import { useMemo, useState } from "react";
import { Link } from "wouter";
import { Check, ImagePlus, LayoutDashboard, Save, Settings2, Trash2, UploadCloud } from "lucide-react";

// Style reminder: editorial control room in cacao, burgundy, toasted orange and maize.
type Product = { id: string; name: string; description: string; price: number; category: string; available: boolean; sortOrder: number; image: string };
const initialProducts: Product[] = [
  { id: "simple", name: "Panini Simple", description: "Pain toasté, sauce maison", price: 500, category: "Panini", available: true, sortOrder: 1, image: "/images/panini-simple.jpg" },
  { id: "viande", name: "Panini Viande", description: "Viande assaisonnée, sauce maison", price: 1000, category: "Panini", available: true, sortOrder: 2, image: "/images/panini-viande.jpg" },
  { id: "jambon", name: "Panini Jambon", description: "Jambon, salade et sauce maison", price: 1500, category: "Panini", available: true, sortOrder: 3, image: "/images/panini-jambon.jpg" },
  { id: "jambon-fromage", name: "Panini Jambon Fromage", description: "Jambon, fromage fondant et sauce maison", price: 2000, category: "Panini", available: true, sortOrder: 4, image: "/images/panini-jambon-fromage.jpg" },
  { id: "chawarma", name: "Chawarma Poulet", description: "Poulet mariné, salade et sauce blanche", price: 1500, category: "Chawarma", available: true, sortOrder: 5, image: "/images/chawarma-poulet.jpg" },
];
const initialSite = { brandName: "Panini de la Gloire", address: "Adjamé Bingerville • En face de BonPrix", phone: "+2250574971022", openingHours: "Tous les jours · 10h00 — 22h00", heroTitle: "La pause qui mérite une ovation.", heroDescription: "Choisissez votre recette, composez votre panier et confirmez votre commande directement sur WhatsApp." };

export default function Admin() {
  const [authenticated, setAuthenticated] = useState(false);
  const [pin, setPin] = useState("");
  const [authError, setAuthError] = useState("");
  const [publishState, setPublishState] = useState("");
  const [products, setProducts] = useState<Product[]>(initialProducts);
  const [site, setSite] = useState(initialSite);
  const [active, setActive] = useState<"products" | "site">("products");
  const [saved, setSaved] = useState(false);
  const [selectedId, setSelectedId] = useState(products[0].id);
  const selected = useMemo(() => products.find((item) => item.id === selectedId) ?? products[0], [products, selectedId]);

  const updateProduct = (patch: Partial<Product>) => setProducts((items) => items.map((item) => item.id === selected.id ? { ...item, ...patch } : item));
  const handleImage = (file?: File) => { if (!file) return; const reader = new FileReader(); reader.onload = () => updateProduct({ image: String(reader.result) }); reader.readAsDataURL(file); };
  const saveDraft = () => { localStorage.setItem("panini-gloire-editor-draft", JSON.stringify({ products, site })); setSaved(true); window.setTimeout(() => setSaved(false), 2200); };
  const login = async () => { setAuthError(""); const response = await fetch("/api/admin-auth", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ pin }) }); if (response.ok) { setAuthenticated(true); setPin(""); } else setAuthError("PIN incorrect ou service indisponible."); };
  const publish = async () => { setPublishState("Publication en cours…"); const response = await fetch("/api/publish-content", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ products, site, message: "chore: update editorial content" }) }); setPublishState(response.ok ? "Proposition GitHub créée." : "Publication refusée. Vérifiez la session et les secrets Netlify."); };
  if (!authenticated) return <main className="admin-page admin-login-page"><section className="admin-login-card"><p className="eyebrow dark">ESPACE MARINA</p><h1>Ouvrir le comptoir éditorial</h1><p>Le PIN est vérifié côté serveur et n’est jamais intégré au bundle public.</p><label>PIN administrateur<input type="password" inputMode="numeric" value={pin} onChange={(event) => setPin(event.target.value)} onKeyDown={(event) => { if (event.key === "Enter") void login(); }} /></label><button className="primary-button" onClick={() => void login()}>Accéder au dashboard</button>{authError && <p className="auth-error">{authError}</p>}<Link href="/" className="back-link">Retour au site</Link></section></main>;

  return <main className="admin-page"><header className="admin-topbar"><div><p className="eyebrow dark">ESPACE ÉDITORIAL</p><h1>Le comptoir de Marina</h1></div><Link href="/" className="back-link">Voir le site</Link></header><div className="admin-layout"><aside className="admin-sidebar"><div className="admin-brand"><span className="admin-brand-dot" /><strong>Panini de la Gloire</strong></div><button className={active === "products" ? "admin-nav active" : "admin-nav"} onClick={() => setActive("products")}><LayoutDashboard size={18} /> Produits</button><button className={active === "site" ? "admin-nav active" : "admin-nav"} onClick={() => setActive("site")}><Settings2 size={18} /> Informations du site</button><div className="admin-sidebar-note"><strong>Publication GitHub</strong><p>Chaque validation prépare un commit versionné avant reconstruction Netlify.</p></div></aside><section className="admin-content">{active === "products" ? <><div className="admin-heading"><div><p className="eyebrow dark">CATALOGUE</p><h2>Les recettes du jour</h2></div><button className="primary-button" onClick={saveDraft}>{saved ? <><Check size={17} /> Brouillon enregistré</> : <><Save size={17} /> Enregistrer le brouillon</>}</button></div><div className="admin-workspace"><div className="admin-list">{products.map((item) => <button className={selectedId === item.id ? "admin-product-row active" : "admin-product-row"} key={item.id} onClick={() => setSelectedId(item.id)}><span className="admin-thumb" style={{ backgroundImage: `url(${item.image})` }} /><span><strong>{item.name}</strong><small>{item.price} F · {item.available ? "Disponible" : "Masqué"}</small></span><span className={item.available ? "status-dot" : "status-dot off"} /></button>)}</div><div className="admin-form-card"><div className="admin-form-heading"><div><p className="eyebrow dark">FICHE PRODUIT</p><h3>{selected.name}</h3></div><button className="delete-button" onClick={() => setProducts((items) => items.filter((item) => item.id !== selected.id))} aria-label="Supprimer le produit"><Trash2 size={17} /></button></div><label>Nom du produit<input value={selected.name} onChange={(event) => updateProduct({ name: event.target.value })} /></label><label>Description<textarea value={selected.description} onChange={(event) => updateProduct({ description: event.target.value })} rows={3} /></label><div className="admin-form-grid"><label>Prix (FCFA)<input type="number" min={0} step={50} value={selected.price} onChange={(event) => updateProduct({ price: Number(event.target.value) })} /></label><label>Catégorie<select value={selected.category} onChange={(event) => updateProduct({ category: event.target.value })}><option>Panini</option><option>Chawarma</option></select></label></div><label className="admin-check"><input type="checkbox" checked={selected.available} onChange={(event) => updateProduct({ available: event.target.checked })} /> Produit visible sur la carte</label><div className="upload-zone"><ImagePlus size={24} /><strong>Remplacer la photo</strong><span>JPG, PNG ou WebP · aperçu local immédiat</span><label className="upload-button"><UploadCloud size={16} /> Choisir une image<input type="file" accept="image/png,image/jpeg,image/webp" onChange={(event) => handleImage(event.target.files?.[0])} /></label></div></div></div></> : <><div className="admin-heading"><div><p className="eyebrow dark">IDENTITÉ ET SERVICE</p><h2>Les informations visibles</h2></div><button className="primary-button" onClick={saveDraft}><Save size={17} /> Enregistrer le brouillon</button></div><div className="site-form-card">{Object.entries(site).map(([key, value]) => <label key={key}>{key === "brandName" ? "Nom de l’enseigne" : key === "address" ? "Adresse" : key === "phone" ? "WhatsApp" : key === "openingHours" ? "Horaires" : key === "heroTitle" ? "Accroche principale" : "Description hero"}{key === "heroDescription" ? <textarea rows={4} value={value} onChange={(event) => setSite({ ...site, [key]: event.target.value })} /> : <input value={value} onChange={(event) => setSite({ ...site, [key]: event.target.value })} />}</label>)}</div></>}<div className="publish-strip"><div><strong>Prêt à publier ?</strong><span>Le prochain envoi créera une proposition de commit GitHub.</span></div><button className="publish-button" onClick={() => void publish()}><UploadCloud size={18} /> Préparer la publication</button></div></section></div></main>;
}
